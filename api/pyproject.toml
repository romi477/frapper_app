[project]
name = "frapper-api"
version = "1.0.0"
description = "Frapper FastAPI service"
requires-python = ">=3.11,<3.12"
dependencies = [
    "frapper-core",
    "fastapi==0.110.2",
    "uvicorn==0.29.0",
    "pydantic-settings==2.2.1",
    "pony==0.7.19",
]

[tool.uv]
package = false

[tool.uv.sources]
frapper-core = { workspace = true }
