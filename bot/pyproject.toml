[project]
name = "frapper-bot"
version = "1.0.0"
description = "Frapper Telethon bot"
requires-python = ">=3.11,<3.12"
dependencies = [
    "frapper-core",
    "telethon==1.35.0",
    "redis==5.0.4",
    "pydantic-settings==2.2.1",
]

[tool.uv]
package = false

[tool.uv.sources]
frapper-core = { workspace = true }
